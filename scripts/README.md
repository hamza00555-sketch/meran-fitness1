# حزمة الأيقونات — التوليد والنشر

الأيقونات لا تُخزَّن في هذا الريبو. تُولَّد، تُرفع على Cloudflare R2، ويصفها
ملف `manifest.json` واحد. التطبيق يقرأ المانيفست، ينزّل ما ينقصه، ويتحقق من
كل ملف قبل حفظه.

## دورة العمل

```bash
# ١. اطبع البرومبتات (١١٠ أيقونة، أسلوب واحد)
npm run pack:prompts                 # أو: node scripts/prompts.mjs --json

# ٢. ولّد الصور واحفظها باسم المعرّف: assets-src/flame.png ...
#    (للتجربة بدون فن حقيقي: node scripts/gen-placeholders.mjs assets-src)

# ٣. ابنِ الحزمة
npm run pack:build -- --base https://assets.meran.app/

# ٤. انشرها
npm run pack:publish                 # جرّب --dry أولاً
```

`pack/` و`assets-src/` مستثناة من git.

## لماذا اسم الملف هو التجزئة

كل ملف يُنشر باسم `i/<sha256>.webp`. ثلاث نتائج:

- **التحديث رخيص.** التطبيق يقارن التجزئات؛ تغيّرت ٥ أيقونات من ١١٠ ⇒ ٥ ملفات.
- **الاستئناف مجاني.** «هل عندي هذه التجزئة محفوظة؟» هو كل شرط الاستكمال.
- **الكاش أبدي بأمان** (`immutable`) لأن الملف المتغيّر رابط مختلف.

المانيفست وحده قصير العمر (`max-age=60`) — هو المؤشر الذي يجب أن يبقى طازجاً.
ولذلك يُرفع **آخر شيء**: حتى تنتهي كل الملفات، لا شيء يشير إليها، وفشل الرفع
يترك الحزمة القديمة تعمل.

## إعداد البَكِت — مرة واحدة

1. **نطاق مخصص** (R2 → Settings → Public access → Custom domain).
   لا تستخدم رابط `*.r2.dev`: محدود المعدل وغير مخصص للإنتاج.
2. **CORS** — التطبيق يقرأ البايتات بـ`fetch` ليحسب التجزئة، فلا يكفي وصول
   `<img>`:

```json
[{
  "AllowedOrigins": ["https://<your-app>.vercel.app", "http://localhost:4173", "http://localhost:5173"],
  "AllowedMethods": ["GET", "HEAD"],
  "AllowedHeaders": ["*"],
  "ExposeHeaders": ["content-length", "etag"],
  "MaxAgeSeconds": 86400
}]
```

3. `VITE_ASSETS_MANIFEST_URL` في متغيّرات بيئة Vercel، وفي `.env` محلياً.

## إضافة أيقونة لاحقاً

أضف المعرّف في `src/assets/ids.js`، ولّد الصورة، وأعد البناء والنشر. لا يحتاج
التطبيق إعادة نشر **إلا** إذا كان الموضع الذي تُرسم فيه جديداً في الواجهة —
المانيفست يستطيع تغيير فنّ معرّف قائم أو سحبه، لكنه لا يخترع مكاناً جديداً
لرسمه.
